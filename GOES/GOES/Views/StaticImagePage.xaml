<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:GOES.ViewModels"
             xmlns:p="clr-namespace:Xamarin.Forms.PinchZoomImage;assembly=Xamarin.Forms.PinchZoomImage"
             x:Class="GOES.Views.StaticImagePage"
             Shell.NavBarIsVisible = "True"
             Title="Image">
    <ContentPage.Resources>
        <ResourceDictionary>
            <vm:DateTimeConverter x:Key="DateTimeConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Refresh" Clicked="Button_Clicked" />
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <StackLayout VerticalOptions="Center">
            <Label Text="{Binding ModifiedDate, Converter={StaticResource DateTimeConverter}, Mode=OneWay}" HorizontalOptions="Center" />
            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />
            <p:PinchZoom>
                <Image x:Name="ImageView" VerticalOptions="Center" HorizontalOptions="Center" Aspect="AspectFit" />
            </p:PinchZoom>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>